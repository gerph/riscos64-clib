## 0.22 (08 Nov 2025)

### Added
- stdlib: `mktemp` now implemented.

### Changed
- linking: `riscos64-addsignatures` now trims the `.hidden ` labels from hidden symbols.

### Fixed
- stdio: `fgetc` and `_kernel_osbget` no longer return 0 for every call.

